package longestpalindromic

func longestPalindrome(s string) string {

	// define DP -> index: palindromic len, value
	dp = make([][]bool, 0) // i ~ j (from idx-i to idx-j is palindrome)

	return ""
}
